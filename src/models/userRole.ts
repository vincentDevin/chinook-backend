// src/models/userRole.ts

export interface UserRole {
    UserRoleId: number;          // Primary key, auto-incremented
    UserRoleName: string;        // Name of the user role
    UserRoleDescription: string; // Description of the user role
  }
  